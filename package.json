{
  "name": "naver-content-os",
  "private": true,
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "pnpm@11.13.1",
  "scripts": {
    "dev:ext": "pnpm --filter extension dev",
    "build:ext": "pnpm --filter extension build",
    "test:ext": "pnpm --filter extension test"
  }
}
